قبل از پاسخ دادن عجله نکن. اول مسئله، سورس، فایل‌ها، وابستگی‌ها، ریسک‌ها و اثرات جانبی را عمیق تحلیل کن. اگر شواهد کافی از سورس، لاگ، diff یا تست نداری، ادعای قطعی نکن. خروجی سطحی، حدسی، تبلیغاتی یا سریع نده. هر پیشنهاد کدنویسی باید کوچک، قابل تست، قابل rollback و سازگار با WordPress/WooCommerce/PHP standards باشد. اگر چیزی خارج از scope است فقط گزارش بده و بدون تأیید من patch یا refactor پیشنهاد نکن. قبل از ارائه راه‌حل، تفاوت Fact، Assumption، Risk و Recommendation را مشخص کن.

Continue the PlaySyncer project with only:

PS-03D5-6A — Account Write Frontend Core Reconstruction

Input packages:

Approved baseline:
playsyncer-ps03d5-5-f1-closed.zip

Unapproved donor reference:
playsyncernv4-main.zip

Do not continue directly from playsyncernv4-main.zip.

Extract both packages separately.

Use the approved PS-03D5-5-F1 package as the only working baseline.

Use v4 only as a read-only donor for reusable frontend code.

Do not bulk-copy the v4 Account Write directory.

Do not commit.
Do not start PS-03D5-6B.
Do not start PS-03D5-7.

## 1. Baseline gate

Before editing, verify from the approved baseline:

- PS-03D5-5-F1 is closed
- public Account mutation routes remain disabled
- Account Delete is fail-closed
- Create, Update, and Status Override backend contracts exist
- generated Account mutation hooks exist
- the active frontend remains read-only

If any item is false, stop with:

PS-03D5-6A — BASELINE CONFLICT

## 2. Donor inspection

Inspect only the PS-03D5-6 frontend work in v4.

Create a short salvage table containing:

- donor file
- reusable as-is
- reusable after correction
- rejected
- reason
- target baseline file

Known v4 defects that must not be copied unchanged:

- reading API errors from error.response instead of error.data
- detecting ACCOUNT_OPS_DISABLED from Error.message
- rendering raw error messages
- incomplete Password and Backup Code clearing
- Date.parse-only birth-date validation
- invented safe DTO fields
- requiring currentOverride when the DTO does not expose it
- importing or calling Delete hooks
- mounting write UI in active pages

## 3. Implement isolated Account Write UI

Create or complete:

artifacts/playsyncer/src/features/account-write/

Implement only:

- Create Account dialog
- Edit Account dialog
- Duplicate Warning dialog
- Status Override dialog
- Delete Unavailable dialog
- validation helpers
- safe API error parser
- mutation adapters
- focused frontend tests

The feature must not be imported by the active router, GameDetailPage, AccountCardReadOnly, AccountDetailsReadOnly, or any mounted production component.

Do not add feature flags, hidden routes, environment switches, or activation bypasses.

## 4. Create Account

Approved fields:

- psnEmail
- psnPassword
- emailPassword
- onlineId
- birthDate
- familyManagementEmail
- backupCodes

Requirements:

- gameId comes from parent context
- gameId is not editable
- statusOverride is not available during Create
- identifiers are not editable
- at least one non-empty Backup Code is required
- Backup Code rows can be added and removed
- prevent duplicate submit while pending
- clear Passwords and Backup Codes after success and every close path

## 5. Edit Account

Use only fields actually present in the generated safe Account DTO.

Requirements:

- never prefill Passwords
- unavailable email values remain empty replacement fields
- empty Password or replacement email means no change
- submit changed fields only
- do not edit Backup Codes
- do not submit immutable identifiers
- clear sensitive values after success, close, external open=false, Account switch, and reopen

Do not extend the safe DTO with invented fields.

## 6. Safe API errors

Inspect the real ApiError in custom-fetch.ts.

Read structured errors from:

- error.data
- error.status

Duplicate Warning:

- data.code === "DUPLICATE_WARNING"
- duplicate fields come from data.detail.duplicateFields

Runtime disabled:

- data.code === "ACCOUNT_OPS_DISABLED"

Approved Persian disabled message:

عملیات مدیریت اکانت‌ها فعلاً فعال نیست

Approved generic message:

عملیات با خطا مواجه شد. لطفاً دوباره تلاش کنید

Never render raw Error.message, URLs, HTTP details, server responses, SQL, Passwords, or Backup Codes.

## 7. Duplicate flow

For Create and Update:

1. submit without confirmed: true
2. DUPLICATE_WARNING opens the confirmation dialog
3. display only safe Persian field labels
4. never display values or Account IDs
5. cancel sends no retry
6. confirm sends one retry with confirmed: true
7. editing the form invalidates the warning
8. closing the form clears warning state
9. a second duplicate response must not create an automatic retry loop

Tests must use the real ApiError.data shape.

## 8. Status Override

Offer only:

- SOLD
- INACTIVE
- Clear Override

Do not offer:

- AVAILABLE
- PARTIALLY_SOLD

Clear Override must send null.

Do not require currentOverride unless it exists in the generated safe DTO.

Do not claim whether the current status is manually overridden.

## 9. Delete Unavailable

Do not import or call:

- deleteAccount
- useDeleteAccount

Show only this user-facing meaning:

حذف دائمی اکانت فعلاً امکان‌پذیر نیست. برای خارج‌کردن اکانت از چرخه فروش، وضعیت آن را غیرفعال کنید.

The dialog may direct the user toward INACTIVE.

It must send zero DELETE requests and must not remove the Account locally.

## 10. Validation

Birth date must:

- match YYYY-MM-DD
- represent a real Gregorian date
- reject impossible dates such as 2026-02-31
- support valid leap years

Do not rely only on Date.parse.

## 11. Focused tests

Add tests for:

- Create request shape and validation
- Edit changed-fields-only behavior
- real ApiError.data duplicate flow
- ACCOUNT_OPS_DISABLED safe message
- generic unknown-error message
- strict birth-date validation
- Password and Backup Code clearing
- SOLD, INACTIVE, and null override payloads
- zero DELETE requests
- Account Write feature not mounted in active pages
- zero real network requests

Use existing Vitest and Testing Library conventions.

Do not add dependencies.

## 12. Scope restrictions

Do not change:

- backend services
- backend routes
- OpenAPI
- generated API files manually
- database schema
- migrations
- lockfile
- dependencies
- encryption
- Account identifiers
- Capacity templates
- infrastructure
- active read-only pages

Do not implement Auth, RBAC, Audit, Secret Reveal, Assignment, Orders, Capacity mutations, Hard Delete, WordPress, or WooCommerce integration.

Do not run db:migrate.

## 13. Logs and validation

Create these files before final reporting:

- reports/ps03d5-6a-execution-log.md
- reports/ps03d5-6a-salvage-matrix.md
- reports/ps03d5-6a-validation-full.txt
- reports/ps03d5-6a.diff
- reports/ps03d5-6a-manifest.txt

The execution log must be chronological and include:

- inspected files
- accepted donor code
- rejected donor code
- files changed
- failures
- diagnoses
- corrections
- reruns

Capture complete stdout, stderr, exit code, and duration for:

- pnpm install --frozen-lockfile
- pnpm run typecheck
- pnpm --filter @workspace/playsyncer run test
- pnpm --filter @workspace/playsyncer run test -- --reporter=verbose
- PORT=24351 BASE_PATH=/ pnpm --filter @workspace/playsyncer run build
- git diff --check, when Git metadata exists

Do not hide failed attempts or replace raw output with a summary table.

## 14. Documentation

Update docs/CURRENT_PHASE.md to:

Current sub-stage:
PS-03D5-6A — Account Write Frontend Core Reconstruction

Current status:
IMPLEMENTATION COMPLETE, AWAITING SOURCE REVIEW

Next:
PS-03D5-6B — Frontend Safety and Final Verification

PS-03D5-7 remains NOT AUTHORIZED.

Do not mark PS-03D5-6 approved or closed.

## 15. Review package

Create a real ZIP:

playsyncer-ps03d5-6a-account-write-core-review.zip

The ZIP must contain the complete final source based on the approved baseline.

Exclude:

- .git
- node_modules
- dist
- build
- coverage
- caches
- .env
- Secrets
- prompt files
- attached_assets/Pasted-*
- old or nested ZIP files
- donor v4 directory

Verify with:

- file
- unzip -t
- exact byte size
- SHA-256

A Git LFS pointer is a packaging failure.

## Final response

Return:

## Final Status

Use exactly one:

- PS-03D5-6A — IMPLEMENTATION COMPLETE, AWAITING SOURCE REVIEW
- PS-03D5-6A — BASELINE CONFLICT
- PS-03D5-6A — PARTIAL IMPLEMENTATION
- PS-03D5-6A — CONTRACT CONFLICT
- PS-03D5-6A — TEST OR BUILD FAILURE
- PS-03D5-6A — SENSITIVE DATA BLOCKER
- PS-03D5-6A — RUNTIME ACTIVATION BLOCKER
- PS-03D5-6A — SCOPE VIOLATION
- PS-03D5-6A — PACKAGING FAILURE

Then report:

- baseline verification
- salvage summary
- files changed
- implemented UI
- tests and exact counts
- complete validation results
- sensitive-data proof
- runtime-isolation proof
- execution log summary
- failures and fixes
- ZIP filename, size, SHA-256, and unzip result
- git status --short

Do not commit.
Do not start PS-03D5-6B automatically.